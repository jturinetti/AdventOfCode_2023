{
    "version": "2.0.0",
    "tasks": [
        {
            "runOptions": {"runOn":"folderOpen"},
            "label": "Open Terminal",
            "command": "${command:workbench.action.createTerminalEditorSide}",
            "group": "build"
        }
    ]
}